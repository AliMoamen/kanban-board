import "../styles/Spinner.scss"; // Optional: Add your styles for the spinner
const Spinner = () => {
  return (
    <div className="spinner">
      <div className="loader"></div>
    </div>
  );
};

export default Spinner;
